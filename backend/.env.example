# Copy to backend/.env for local development only. Never commit backend/.env.
# Inject production credentials through the deployment platform's secret manager.
MONGO_URL=mongodb://localhost:27017
DB_NAME=coach_max_dev
CORS_ORIGINS=http://localhost:3000

# Secret values: leave blank in source-controlled files.
EMERGENT_LLM_KEY=
RESEND_API_KEY=
THINKIFIC_API_KEY=

# Deployment-specific, non-secret configuration.
SENDER_EMAIL=
NOTIFICATION_EMAIL=
SUPER_ADMIN_EMAIL=
THINKIFIC_SUBDOMAIN=
