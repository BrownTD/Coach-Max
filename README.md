# Coach Max

Runtime credentials are managed outside source control. See
[Secrets management](docs/SECRETS_MANAGEMENT.md) for the approved Infisical
environment structure, local injection command, access model, and rotation
procedure. Use `backend/.env.example` and `frontend/.env.example` only as
sanitized variable-name contracts.
