# Public build-time configuration only. React embeds every REACT_APP_* value in
# the browser bundle. Never add credentials, secret-manager authentication, or
# server-only configuration here.
REACT_APP_BACKEND_URL=http://localhost:8000
